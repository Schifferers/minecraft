---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minecraft

resources:
  - service-account.yaml
  - statefulset.yaml
  - service.yaml
  - ingress-route.yaml

configMapGenerator:
  - name: vanilla-files
    files:
      - server.properties
      - user_jvm_args.txt

commonLabels:
  minecraft: vanilla
  type: release
  version: "1.19.2"
